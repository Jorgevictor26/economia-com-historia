import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF5C1E2F);
  static const primaryHover = Color(0xFF8A3F50);
  static const accentGold = Color(0xFFD4AF37);
  static const blush = Color(0xFFF2E6E9);
  static const roseMuted = Color(0xFFC48A96);
  static const soft = Color(0xFFF5F5F5);
  static const line = Color(0xFFE0E0E0);
  static const muted = Color(0xFFBDBDBD);
  static const copyMuted = Color(0xFF9E9E9E);
  static const copy = Color(0xFF616161);
  static const ink = Color(0xFF212121);
  static const sand = Color(0xFFE6D5B8);
  static const paper = Color(0xFFE0E0E0);

  static const background = soft;
  static const cardBackground = Colors.white;
  static const inputFill = Colors.white;
  static const surface = blush;
  static const secondary = accentGold;
  static const gold = accentGold;
  static const textBordeaux = primaryHover;
  static const textDark = ink;
  static const textMedium = copy;
  static const textLight = copyMuted;
  static const borderSoft = line;
  static const borderColor = roseMuted;
  static const iconMuted = muted;
  static const disabledBackground = muted;
  static const primaryDark = primary;
  static const secondaryDark = accentGold;

  static const googleBlue = Color(0xFF4285F4);
  static const googleGreen = Color(0xFF34A853);
  static const googleYellow = Color(0xFFFBBC05);
  static const googleRed = Color(0xFFEA4335);

  static const success = Color(0xFF2E7D32);
  static const successSoft = Color(0xFFE8F5E9);
}
